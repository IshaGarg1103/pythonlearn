#question 2
#function to calculate factorial

def num_fact(num):
    fact=1
    for i in range(1,len(list)+1):
        t=total+list[i]
    return total
list=eval(input("Enter the list with numbers seperated by commas :"))
mean=num_mean(list)/len(list)
print("mean of the elements of the list is :",mean)